# Supabase Configuration
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Server Configuration
PORT=
NODE_ENV=

# JWT Secret (for additional security if needed)
JWT_SECRET=

# Cache Configuration
# Leave empty for in-memory cache (development)
# Set to Redis URL for production (e.g., redis://localhost:6379 or ElastiCache endpoint)
REDIS_URL=
